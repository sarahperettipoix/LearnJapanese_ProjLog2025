/*// Sélectionner tous les éléments avec la classe "circle hiragana" et ajouter l'événement
document.querySelectorAll(".circle.hiragana").forEach(function(element) {
    element.addEventListener("click", function() {
        window.location.href = "/hiragana?type=hiragana";
    });
});

// Sélectionner tous les éléments avec la classe "circle katakana" et ajouter l'événement
document.querySelectorAll(".circle.katakana").forEach(function(element) {
    element.addEventListener("click", function() {
        window.location.href = "/katakana?type=katakana";
    });
});

// Affichage de l'alerte
//window.alert("ASNONOAS");
*/

// pas nécessaire mais plutot cool